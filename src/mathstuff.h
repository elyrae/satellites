#ifndef MATHSTUFF_H
#define MATHSTUFF_H

#include <cmath>

namespace MathStuff {
    double degreesToRad(const double x);
    // {
    // 	return x * M_PI / 180.0;
    // }
    double radToDegrees(const double x);
    // {
    // 	return x * 180.0 / M_PI;
    // }
}

#endif // MATHSTUFF_H
